<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tvStopWatch"
        android:layout_width="415dp"
        android:layout_height="81dp"
        android:layout_marginTop="112dp"
        android:gravity="center"
        android:textColor="@color/black"
        android:textSize="40sp"
        android:textStyle="bold"
        android:background="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnStartWatch"
        android:layout_width="158dp"
        android:layout_height="50dp"
        android:layout_marginTop="60dp"
        android:onClick="onstart"
        android:text="Start"
        app:layout_constraintEnd_toEndOf="@+id/btnResetWatch"
        app:layout_constraintHorizontal_bias="0.501"
        app:layout_constraintStart_toStartOf="@+id/btnStopWatch"
        app:layout_constraintTop_toBottomOf="@+id/tvStopWatch" />

    <Button
        android:id="@+id/btnStopWatch"
        android:layout_width="158dp"
        android:layout_height="50dp"
        android:layout_marginTop="32dp"
        android:onClick="onstop"
        android:text="Stop"
        app:layout_constraintEnd_toStartOf="@+id/btnResetWatch"
        app:layout_constraintHorizontal_bias="0.16"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btnStartWatch" />

    <Button
        android:id="@+id/btnResetWatch"
        android:layout_width="158dp"
        android:layout_height="50dp"
        android:onClick="onreset"
        android:text="Reset"
        app:layout_constraintBaseline_toBaselineOf="@+id/btnStopWatch"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.869"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>